# CHANGELOG

## v0.1.0-alpha

- 현재 버전명을 `v0.1.0-alpha`로 고정했습니다.
- 기존 정적 MVP 기준 백업을 `backups/v0.1.0-alpha-baseline/`에 남겼습니다.
- 시작 화면을 워크스페이스 목록, 새 워크스페이스 만들기, 워크스페이스 파일 가져오기 중심으로 정리했습니다.
- 헤더가 시작 화면에서는 `Layoutstudio SPACE · v0.1.0-alpha`, 작업 화면에서는 `워크스페이스명 · 도면명`을 표시하도록 정리했습니다.
- 상단 시간 표시를 초 단위로 갱신하고, 시간 기준 자동 테마 전환과 수동 테마 우선 적용을 추가했습니다.
- 도면 이미지를 잠긴 reference floorplan layer로 분리하고, 워크스페이스 데이터는 mm 기반 실측 좌표 중심으로 정리했습니다.
- 기존 JSON 호환을 위해 `xCm`, `yCm`, `widthCm`, `depthCm` 필드는 유지하면서 `xMm`, `yMm`, `widthMm`, `depthMm`을 우선 저장합니다.
- OCR 치수 후보를 자동 확정하지 않고 사용자가 확인하는 흐름으로 바꿨습니다.
- 도면 기준 영역을 마우스로 드래그/리사이즈하고, 좌/우/상/하 여백 수치로도 보정할 수 있게 했습니다.
- 기준 영역 수치 입력 중 모달이 다시 그려져 값 입력이 튕기던 문제를 줄였습니다.
- 500mm/1000mm 실측 그리드와 mm 기준 화면 배율 계산을 추가했습니다.
- 향후 실내 구조 재구성을 위해 `structureLayer.rooms`를 추가하고, 방/영역 사각형 생성/이동/수정/삭제를 지원합니다.
- 워크스페이스 생성 마법사에 초기 가구 보관함 설정 단계를 추가했습니다.
- 기본 가구 프리셋을 mm 치수와 높이 정보 중심으로 정리하고, 마법사에서 선택/수정할 수 있게 했습니다.
- 보관함 가구에 제조사명, 모델명, 높이 필드를 추가했습니다.
- 보관함 영역을 고정 높이 스크롤 구조로 정리하고, 가구 카드 이름/종류/크기/색상/연결 상태 표시를 개선했습니다.
- 보관함 가구 추가, 수정, 복제, 삭제와 기존 배치 항목 독립 유지를 보강했습니다.
- 배치된 가구의 이름, 종류, 크기, 실측 좌표, 회전, 색상, 메모, 잠금을 우측 패널에서 수정할 수 있게 했습니다.
- Fabric.js 기본 회전 핸들을 숨기고 우측 패널 회전 버튼과 키보드 단축키를 유지했습니다.
- 워크스페이스 설정과 삭제 문구를 워크스페이스 중심으로 정리했습니다.
- LocalStorage 자동 저장, 최근 작업 복구, JSON 가져오기/내보내기 호환을 유지했습니다.
- 앱 내부에서 읽을 수 있는 이용 안내, 개인정보처리방침, 오픈소스 라이선스 안내를 보강했습니다.
- Copyright 문구를 `Copyright 2026 ONESTUDIO. All rights reserved.`로 통일했습니다.
- ONESTUDIO 시스템 푸터를 추가했습니다.
- 운영 저장소 도입을 위한 SQL 스키마와 보안 정책 초안을 유지했습니다.
